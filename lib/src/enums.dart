/// Controls how radius values (.r) scale.
enum RadiusMode {
  /// Scale radius using width scale factor.
  width,

  /// Scale radius using height scale factor.
  height,

  /// Scale radius using the shortest side (recommended).
  min,
}
